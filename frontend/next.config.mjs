/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"res.cloudinary.com","www.instagram.com"], // Add other domains as needed
      },
};

export default nextConfig;
